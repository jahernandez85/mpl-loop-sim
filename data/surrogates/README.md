# Surrogate Models

Serialized surrogate / ML closure models consumed by future backend or
correlation implementations.

Will be populated in Phase 3+ when surrogate correlations are introduced.
Each surrogate artifact must record the training data provenance and the
`ReproducibilityTuple` hash of the run that generated it.
