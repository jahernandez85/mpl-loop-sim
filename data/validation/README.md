# Validation Data

Reference data sets used for literature-case validation (Phase 12).

Each subdirectory should contain:
- The digitized experimental or reference data in CSV format.
- A `metadata.yaml` describing the source, units, and uncertainty.
- The `ReproducibilityTuple` YAML used to reproduce the mpl_sim run.
