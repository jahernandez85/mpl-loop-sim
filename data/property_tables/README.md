# Property Tables

CSV property tables consumed by `TabulatedPropertyBackend`.

**Status: PENDING-DATA** — 29 property CSV files are required before
`TabulatedPropertyBackend` can be instantiated. Files must be placed here
and registered in the backend configuration before Phase 2 tests can run.

Expected naming convention: `<fluid_id>_<property>.csv`
